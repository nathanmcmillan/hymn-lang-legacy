class foo<k,v>
    size k
    name v

const thingy = "foo!"

def foo.set_name(name v)
    echo(thingy)
    self.name = name

def foo.get_size() k
    return self.size

def main
    echo(thingy)
    f = foo (
        size: 22.33
        name: "zoob"
    )
    echo(f.size)
    echo(f.get_size())
    echo(f.name)
    f.set_name("goob")
    echo(f.name)
