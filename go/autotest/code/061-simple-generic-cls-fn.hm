type foo<k, v>
	size k
	name v

foo.set_name(name v)
	self.name = name

foo.get_size() k
	return self.size

main
	f = foo(
		size:22.33
		name:"zoob")
	echo(f.size)
	echo(f.get_size())
	echo(f.name)
	f.set_name("goob")
	echo(f.name)
