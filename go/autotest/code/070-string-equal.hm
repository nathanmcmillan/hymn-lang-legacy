main
    a = "foo"
    b = "foo"
    c = "foo"
    d = "bar"
    
    if a == b
        echo("good")
    else
        echo("bad")

    if a != b
        echo("bad")
    else
        echo("good")

    if c == d
        echo("bad")
    else
        echo("good")

    if c != d
        echo("good")
    else
        echo("bad")

    if not c == d
        echo("good")
    else
        echo("bad")

    if not c != d
        echo("bad")
    else
        echo("good")
