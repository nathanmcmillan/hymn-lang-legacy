main
    content = cat("071-cat.c")
    iterate i, c in content
        echo(str(i) + ": " + str(c))
        if i == 19
            break
    echo("done!")
    echo("content length = " + str(len(content)))
