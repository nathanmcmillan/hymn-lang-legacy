main
  for i = 0, i < 5, i += 1
    x = 2
    echo(i)
  (* if we know i or x will not be used later, can declare in C scope *)
  echo(x)
  z = 3
  if x = 2
    b = "foo"
    if z = 4
      w = "zoom"
    else
      w = "zam"
  else
    b = "bar"
  echo(b)
  (* error w may not have been initialized *)
