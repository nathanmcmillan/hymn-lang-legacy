interface expl
  get() int
  set(value int)

  -- struct expl {
  --   int  func_get);
  --   int  func_get)(int value);
  -- };

  -- struct foo_expl {
  --   int get(foo *self);
  --   void set(foo *self, int value);
  -- };

class foo
  implement expl
  x int

foo.get() int
  return self.x

foo.set(value int)
  self.x = value

class bar
  implement expl
  y int
  
bar.get() int
  return self.y

bar.set(value int)
  self.y = value

class special
  one expl
  two expl

main
  a = foo(x:2)
  echo(a.x)

  b = bar(y:3)
  echo(b.y)

  e = special
  e.one = a
  e.two = b

  e.one.set(4)
  e.two.set(5)

  echo(e.one.get())
  echo(e.two.get())
