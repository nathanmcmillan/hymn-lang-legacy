
def get_num(flag bool) maybe<int>
    if flag
        return 12
    return none

def main()
    x = get_num(true)
    y = get_num(false)
    match x
        none => echo("oh no!")
        some(s) => echo("x :=", s)
    if y is some: echo("oh no!")
