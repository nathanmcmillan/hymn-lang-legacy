type foo
  data int

type special
  has %foo # force not just a pointer

type regular
  has foo

(* this is a comment *)
main
  a = special
  a.has.data = 5 (* no need to allocate, already have mem *)

  (* h = &a.has

  b = foo
  b.data = 3
  echo(b.data)
  c = ref b.data
  c = 6
  echo(c)

  d = $special (* force allocate on stack (no-malloc) *)
  (* d.has.data = 5

  e = $foo (* force allocate on stack *)
  (* e.data = 53
  echo(e.data)
  f = &e.data
  f = 9
  echo(f)

  g = regular
  g.has = !foo *) (* g on heap but has points to a new foo on stack *)
