immutable keywords = set<string>
  "import"
  "macro"

class token
  depth int
  is    string
  value string

class tokenizer
  tokens []token

token.toString() string
  if self.value = ""
    s = "{depth:" + self.depth + ", type:" + self.is + "}"
    return s 
  s = "{depth:" + self.depth + ", type:" + self.is + ", value:" + self.value + "}"
  return s 

digit(c byte) bool
  return false

tokenizer.simpleToken(is string) token
  return self.tokenFor(me.depth, is)

tokenizer.tokenFor(depth int, is string) token
  t = token
  t.depth = depth
  t.is = is
  t.value = ""
  return t
